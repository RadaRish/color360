# Итоговое Исправление Координат и Качества Ретуши - Инструкции

## 📋 Что исправлено в этой версии

### 🎯 Координатная точность
- **Стабильный UV маппинг** без накопления ошибок поворота камеры
- **Упрощенная сферическая проекция** используя стандартные theta/phi углы
- **Центральные координаты** теперь последовательно отображаются в UV (0.5, 0.5)

### 🎨 Качество AI ретуши  
- **guidance_scale: 12.0** - баланс между качеством и естественностью
- **num_inference_steps: 50** - высокое качество обработки  
- **strength: 0.95** - сохранение деталей архитектуры
- **Специализированные промпты** для панорамных архитектурных изображений

### 🔧 Техническая стабильность
- **Автоочистка временных файлов** старше 1 дня
- **Улучшенная диагностика** состояния сервисов
- **Проверка здоровья LaMa API** с детальной отчетностью

## 🚀 Развертывание

### На VPS (рекомендуемый способ):
```bash
# Загрузить изменения на GitHub
git add -A
git commit -m "Итоговое исправление координат и качества AI ретуши"
git push origin main

# На сервере запустить
chmod +x final-coordinate-fix.sh
sudo ./final-coordinate-fix.sh
```

### Локальное тестирование:
```bash
# Для Windows PowerShell
start-dev.bat

# Для Linux/Mac  
chmod +x start-dev.sh
./start-dev.sh
```

## 🔍 Проверка результатов

### Координатная точность
- Кликните по центру экрана в панораме
- В консоли должны появиться UV координаты близкие к (0.5, 0.5)
- При повторном клике координаты должны оставаться стабильными

### Качество ретуши
- Выберите объект для удаления (фонарь, табличка, и т.д.)
- Ретушь должна быть качественной с сохранением архитектурных деталей
- Результат должен выглядеть естественно, без размытия

## 📊 Мониторинг

### Логи приложения
```bash
journalctl -u color360-app -f
```

### Логи LaMa сервиса  
```bash
journalctl -u color360-lama -f
```

### Проверка состояния
```bash
curl http://localhost:5002/health
curl http://localhost:3000/
```

## 🐛 Диагностика проблем

### Если координаты все еще неточные
1. Проверьте, что A-Frame камера инициализирована
2. Убедитесь, что ray casting работает корректно
3. Проверьте логи на ошибки в screenToUV функции

### Если качество ретуши низкое
1. Проверьте статус LaMa сервиса: `systemctl status color360-lama`
2. Убедитесь, что AI параметры применились корректно
3. Проверьте размер загружаемых изображений (должны быть < 200MB)

## 🎯 Ожидаемые результаты

✅ **Координаты**: UV центра экрана стабильно (0.5±0.01, 0.5±0.01)  
✅ **Качество**: Естественная архитектурная ретушь без артефактов  
✅ **Стабильность**: Отсутствие накопления ошибок при множественных операциях  
✅ **Производительность**: Время обработки 5-15 секунд для стандартных панорам